version: '3.4'

services:
  back:
    container_name: back
    build: 
      context: .
      target: back
    volumes:
      - ./backend:/code
    ports:
      - "3333:3333"
  
  front:
    container_name: front
    build: 
      context: .
      target: front
    volumes:
      - ./frontend:/code
    ports:
      - "3000:3000"
    links:
      - back
  